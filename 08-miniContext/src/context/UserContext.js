import React from "react";

const UserContext = React.createContext()

export default UserContext
 

//this context will be used to share user data across components thats why it is called as global context